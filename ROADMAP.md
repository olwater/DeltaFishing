# DeltaFishing 技术路线规划 (Technical Roadmap)

> **当前版本**：v0.2.0  
> **核心定位**：Windows 平台基于物理声学与空间立体声的《三角洲行动》高性能自动垂钓系统（纯外部音频检测，无内存读取、无进程注入、无游戏文件修改）。

---

## 一、 背景与声学物理认知突破

在早期版本及同类开源钓鱼脚本中，咬钩识别通常存在两大核心痛点：
1. **误判率高**：周围其他玩家咬钩、旁人开枪、涉水脚步声极易引发误刺鱼。
2. **漏判率高**：在不同水域、不同音量、混响干扰下，真实咬钩无法达到固定门限。

### 1.1 经验主义误区 vs. 物理频谱真相
过去的算法设计中，常直觉性地误认为咬钩是一声“清脆尖锐的高频金属音”，从而在预处理阶段加入了高通滤波器（High-pass Filter），试图滤除低频。

然而，通过对《三角洲行动》游戏实机提取的咬钩音频样本（如 `assets/bite.wav`）进行短时傅里叶变换（STFT）与全频段能量积分测试，**物理数据彻底推翻了“高频假想”**：

- **发声持续时间**：有效咬钩声仅为一段 **约 250ms ~ 300ms** 的爆发瞬态；
- **真实频段分布**：
  - **峰值频率**：集中在 `237 Hz`、`285 Hz`、`333 Hz`、`481 Hz`、`511 Hz` 等低中频；
  - **能量积分**：
    - `150 Hz ~ 350 Hz`（水底沉滞顿击）：占 **22.6%**
    - `350 Hz ~ 700 Hz`（鱼漂入水下挫）：占 **24.1%**
    - `700 Hz ~ 1500 Hz`（鱼线紧绷切水）：占 **27.8%**
    - `1500 Hz ~ 3000 Hz`：占 **17.2%**
    - `3000 Hz 以上高频`：**仅占 3.4%**！

> **真实声学本质**：  
> 《三角洲行动》的咬钩声不是金属撞击，而是一段典型的**“鱼线瞬间拉紧、鱼漂猛烈下顿带出的低沉顿挫切水声”（水下脉冲冲击音）**。旧高通滤波恰恰把 200~800 Hz 最核心的共鸣基频强行衰减了，反而放大了 2000 Hz 以上的环境水花白噪，直接导致信噪比崩塌。

---

## 二、 渐进式技术演进路线全景图

针对该声学特征，DeltaFishing 制定了由轻到重、层层递进的四阶段演进路线：

```text
┌─────────────────────────────────────────────────────────────────────────────┐
│ 阶段一：声学带通重构与数据标定闭环 (v0.2.0 已完成)                          │
│ • 4 阶 Butterworth 200~1200Hz 咬钩切水带通滤波                             │
│ • 300ms 核心瞬态切片 + 15ms 整流包络跟踪                                    │
│ • 立体声三层判据 (相似度 + 电平 dBFS + 声像差 Pan dB) + 原生右键聚焦降噪    │
│ • 声学标定工坊 (Studio)：双端点区间标注、智能预标、长录音分轮切片、离线仿真 │
│ • 换饵保护与抛竿失败自愈重试                                                │
└──────────────────────────────────────┬──────────────────────────────────────┘
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ 阶段二：深度 DSP 信号处理与环境自适应 (进行中 / 近期规划)                   │
│ • GCC-PHAT 广义互相关：频域归一化互功率谱，抗水声湍流微观相位抖动与混响     │
│ • CFAR 恒虚警率动态自适应门限：根据环境底噪均值与方差自适应浮动门限         │
│ • 高低频能量比门控 (Dual-Band Energy Ratio)：瞬间压制枪炮爆炸与踩水杂音     │
│ • 多模板聚类集成 (Multi-Template Ensemble)：深水/浅滩/雨天多环境正交打分    │
└──────────────────────────────────────┬──────────────────────────────────────┘
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ 阶段三：轻量化 TinyML 边缘神经网络 (中期规划)                               │
│ • 短时梅尔频谱 (Log-Mel Spectrogram) 与 MFCC 音色特征余弦相似度匹配         │
│ • ~50KB-150KB 离线 1D-CNN / Micro-YAMNet 关键词唤醒微模型                  │
│ • Rust 端纯 CPU 轻量推理 (tract-onnx / ort)，单次推理 < 0.5ms               │
│ • 极端枪林弹雨与队友开麦干扰下维持 99%+ 绝对识别率                          │
└──────────────────────────────────────┬──────────────────────────────────────┘
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ 阶段四：全场景垂钓生态自主化与自愈 (远期规划)                               │
│ • 路亚模式全流程声学闭环 (远投落水、收线水阻、顿口冲击、搏鱼起鱼)           │
│ • 挂底卡障识别与自动脱困抖竿                                                │
│ • 拟人化微扰动与全链路容灾自愈                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 三、 各阶段核心技术方案与实现细节

### 阶段一：声学带通重构与数据标定闭环（v0.2.0 已落地）

- **200 Hz ~ 1200 Hz 4 阶巴特沃斯数字带通**：
  - 核心频带（200~1200Hz）保持 0 dB 无损透传；
  - 50~100Hz 低频机械轰鸣与电流杂音衰减 `-14.5 dB ~ -26.9 dB`；
  - 2500Hz 以上高频水流白噪与远距离枪声衰减 `-21.4 dB ~ -42.7 dB`（衰减 90%~99% 以上）。
- **300ms 核心切水瞬态切片**：
  - 去除长音频前后无效静音与长尾水声，杜绝归一化互相关分母稀释。
- **15ms 水涌冲击整流包络**：
  - 解决流体水花微观波形相位随机抖动问题，双通道联合判据。
- **立体声三层几何判据**：
  - **存在判据**：ZNCC 波形与包络双通道相似度；
  - **距离判据**：电平有效窗口 `[-28 dB, -6 dB]`，阻断远方他人的鱼和贴脸爆炸；
  - **方位判据**：左右声道声像差 $\le 5\text{ dB}$，精准锚定正前方准星浮漂。
- **声学标定工坊 (Studio)**：
  - 双端点时间线区间标注（Span Labeling）、支持拖动手柄调整毫秒起止点；
  - 基于能量与带通突变的 0.1s 智能自动初筛；
  - 长录音多轮钓鱼自动智能切片（Auto Slice Rounds）、单竿视口聚焦与缩放平移；
  - 换饵/上饵动画时长标定与抛竿失败拒收音采集；
  - 跨切片离线仿真评测看板与数据驱动推荐黄金门限。

---

### 阶段二：深度 DSP 信号处理与环境自适应（近期规划）

#### 2.1 频域广义互相关与相位变换 (GCC-PHAT)
- **原理**：
  标准的时域互相关（ZNCC）极易受幅度波动与波形微观相位畸变影响。GCC-PHAT（Generalized Cross-Correlation with Phase Transform）在频域中计算互功率谱，并将幅值完全归一化为 1，只对相位差进行逆傅里叶变换（IFFT）：
  $$R_{xy}^{\text{PHAT}}(\tau) = \mathcal{F}^{-1} \left( \frac{X(f) \cdot Y^*(f)}{|X(f) \cdot Y^*(f)|} \right)$$
- **预期优势**：
  - 对背景中缓慢波动的流水声和空间混响具备极强免疫力；
  - 对咬钩瞬态脉冲信号会产生极为尖锐的狄拉克 $\delta$ 尖峰，出峰极其利落，抗相位抖动能力跨越式提升；
  - 计算开销仅需小尺寸 FFT/IFFT，CPU 消耗低于 1%。

#### 2.2 CFAR 恒虚警率动态自适应门限
- **原理**：
  借鉴雷达与声呐目标检测中的 **CA-CFAR (Cell Averaging Constant False Alarm Rate)** 机制：
  - 固定门限在安静水域容易过高导致漏鱼，在嘈杂交火区又容易过低导致误判；
  - 实时维护过去 2~3 秒背景音频的均值 $\mu_{\text{noise}}$ 与标准差 $\sigma_{\text{noise}}$；
  - 动态自适应设定触发门限：
    $$\text{Threshold}_{\text{dyn}} = \mu_{\text{noise}} + \alpha \cdot \sigma_{\text{noise}}$$
- **预期优势**：
  - 无论是静谧无人的内陆湖，还是战火纷飞的沿海码头，无需手动调参，系统自动贴合当前底噪环境。

#### 2.3 高低频能量比门控 (Dual-Band Energy Ratio)
- **物理特征**：
  - 咬钩切水能量主要在 $200 \sim 1000\text{ Hz}$，高频部分能量极其微弱，低/高频能量比通常高于 $4:1$；
  - 枪炮开火、弹壳落地、碎石与踩水脚步声在 $2000 \sim 4000\text{ Hz}$ 具有巨大的能量喷发，能量比通常低于 $1:1$。
- **算法设计**：
  - 实时计算：
    $$\text{Ratio} = \frac{\int_{200}^{1000} |X(f)|^2 df}{\int_{2000}^{4000} |X(f)|^2 df}$$
  - 当高频能量占比超过安全阈值时，直接判定为战场杂音并硬件级旁路丢弃，彻底消除枪声对刺鱼动作的干扰。

#### 2.4 多环境正交模板聚类融合 (Multi-Template Ensemble)
- **方案**：
  - 借助声学标定工坊收集深水区、浅滩、芦苇丛及降雨天气下的真实咬钩音频；
  - 通过 K-Means 或谱聚类提取出 3~5 个最具代表性的正交瞬态模板；
  - 实时并行打分并取融合最大值：
    $$\text{Score} = \max\Big(\text{Score}_{\text{deep}}, \text{Score}_{\text{shallow}}, \text{Score}_{\text{rainy}}\Big)$$
  - 彻底解决单一模板在不同水体环境下的过拟合泛化缺陷。

---

### 阶段三：轻量化 TinyML 边缘神经网络（中期规划）

#### 3.1 从时域波形转向 Log-Mel 梅尔频谱与 MFCC
- **原理**：
  - 模仿人耳听觉感知模型，对 20ms 短时音频切帧并计算 16~32 维 Mel 滤波器组能量与倒谱系数；
  - 无论游戏音频引擎如何加入轻微空间反射或微观混响，咬钩音效的“梅尔频谱时频指纹”依然高度稳定；
  - 使用余弦相似度（Cosine Similarity）比对实时频谱与标准频谱特征图。

#### 3.2 离线 1D-CNN / Micro-YAMNet 关键词唤醒模型 (TinyML)
- **模型设计**：
  - 类似于手机离线语音助手唤醒词（“Hey Siri” / “小爱同学”）架构；
  - 采用深度可分离卷积（Depthwise Separable Conv）构建超轻量 1D-CNN 音频二分类模型；
  - **模型大小**：仅 **50 KB ~ 150 KB**；
  - **输入参数**：过去 300ms 音频的 $32 \times 32$ 梅尔频谱特征图；
  - **输出结果**：咬钩切水事件置信度概率（$0.0 \sim 1.0$）。
- **Rust 端轻量部署**：
  - 采用纯 Rust CPU 深度学习推理引擎（如 `tract-onnx`），无需任何外部驱动与庞大依赖；
  - 单次推理仅耗时 **0.3ms ~ 0.6ms**，内存常驻仅数兆字节；
  - **实测抗噪表现**：即便身边重机枪连续扫射、直升机从头顶掠过、队友在大声开麦交流，神经网络依然能凭借高阶时频特征把混在噪声中的切水声精准剥离，达到 **99%+ 真实识别率**。

---

### 阶段四：全场景垂钓生态自主化与自愈（远期规划）

#### 4.1 路亚玩法全流程声学闭环
- **流程建模**：
  - `远投落水 (Cast Splash)`：感知假饵入水瞬态冲击，开始下沉倒计时；
  - `慢摇收线 (Retrieve Resistance)`：感知假饵在水流中的微弱阻力频段，维持匀速搜寻；
  - `掠食顿口 (Hit Attack)`：检测突发截口硬冲击（瞬态陡峭度远超台钓）；
  - `搏鱼控竿 (Fight Control)`：根据竿梢受力音调高低，自动交替点按收线与放线泄力；
  - `起鱼抄网 (Catch Landing)`：判定鱼体出水声，完成循环。

#### 4.2 智能自愈与环境安全
- **挂底与断线检测**：长时间收线受阻且频谱无鱼挣扎特征时判定挂底，自动执行微抖解线或切刀重置；
- **背包满载与耐久耗尽感知**：通过系统报错音与界面提示音识别背包已满或鱼竿破损，自动鸣音报警；
- **高拟人化输入控制**：抛竿、刺鱼动作引入正态分布延迟（Jitter）与非恒定长按时间，天然阻断基于行为特征的统计检测。

---

## 四、 结语与贡献指南

DeltaFishing 坚持**“数据驱动与声学物理对齐”**的工程哲学。如果你在实战中录制了特殊水域、特殊天气或嘈杂环境下的高价值音频，欢迎通过软件内的 **🎙️ 声学标定工坊** 导出数据集或提出 Pull Request，共同推动抗干扰自动垂钓算法的极致演进！
